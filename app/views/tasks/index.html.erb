<h1>業務一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.status %>  &gt; <%= task.content %></li>
  <% end %>
</ul>
<%= paginate @tasks %>

<%= link_to '新規登録する', new_task_path %>