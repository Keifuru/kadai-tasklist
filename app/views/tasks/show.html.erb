<h1>id: <%= @task.id %> の業務内容ページ</h1>

<p><%= @task.status %></p>
<p><%= @task.content %></p>

<%= link_to '一覧に戻る', tasks_path %>
<%= link_to '内容を変更する', edit_task_path(@task) %>
<%= link_to '削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？'} %>